//The next is just Dummy behaviour.

export function convertUSDToCryptoAmount(USD: number): number {
  //Brant will send me conversion function from usd to crypto
  return USD / 2;
}

export function convertCryptoAmountToUSD(Crypto: number): number {
  //Brant will send me conversion function from usd to crypto
  //make the modifications to do the inverse thing.
  return Crypto * 2;
}
